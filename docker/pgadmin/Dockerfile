FROM dpage/pgadmin4

ENV PGADMIN_DEFAULT_EMAIL=$DEV_PGADMIN_DEFAULT_EMAIL
ENV PGADMIN_DEFAULT_PASSWORD=$DEV_PGADMIN_DEFAULT_PASSWORD
#ENV PGADMIN_ENABLE_TLS=$DEV_PGADMIN_ENABLE_TLS
ENV PGADMIN_LISTEN_PORT=$DEV_PGADMIN_LISTEN_PORT

EXPOSE $DEV_PORT_PGADMIN